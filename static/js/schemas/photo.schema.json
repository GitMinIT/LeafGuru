{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "photo.schema.json",
  "title": "LeafGuru Photo",
  "type": "object",
  "required": [
    "id",
    "plantId",
    "takenAt"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "plantId": {
      "type": "string",
      "format": "uuid"
    },
    "blob": {
      "type": "string",
      "description": "IndexedDB: stored as Blob; export serializes base64 data URL"
    },
    "mimeType": {
      "enum": [
        "image/jpeg",
        "image/png",
        "image/webp"
      ]
    },
    "takenAt": {
      "type": "string",
      "format": "date-time"
    },
    "note": {
      "type": "string",
      "maxLength": 500
    },
    "phaseTag": {
      "enum": [
        null,
        "seedling",
        "vegetative",
        "flowering",
        "harvest",
        "drying",
        "cured"
      ]
    },
    "createdAt": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "updatedAt": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    }
  },
  "additionalProperties": false
}